# 📜 Changelog (변경 이력)

모든 주요 변경 사항은 이 문서에 기록됩니다.  
이 프로젝트는 [Semantic Versioning](https://semver.org/lang/ko/) 규칙을 따릅니다.

---

## [v0.3.7] - 2026-08-23

### 🐛 Fixed (버그 수정)
- **ADB 데몬 자동 실행(Cold Start) 복구**:
  - 로컬 환경(`127.0.0.1`, `localhost`) 및 `start-server` 명령 시 `-H` 플래그를 제외하도록 수정하여, ADB 서버가 꺼져 있는 상태에서도 구버전 도움 없이 정상적으로 백그라운드 ADB 데몬이 자동 기동되도록 해결
  - 한글 윈도우(CP949) 환경에서 ADB 표준 출력 오류 메시지 처리 시 `UnicodeDecodeError` 크래시 방지 (UTF-8 replace 디코딩 적용)
- **템플릿 우선순위 드래그 앤 드롭 실시간 시각적 재배치 반영**:
  - `update_all_template_order` 내 `pack_forget()`을 선행 적용하여 프로그램 재시작 없이도 UI 상에서 템플릿 목록 행의 위아래 순서가 즉시 시각적으로 재배치되도록 수정

### ⚡ Performance (성능 최적화)
- **다중 인스턴스 OpenCV 스레드 폭발 방지 및 CPU 최적화**:
  - OpenCV 내부 워커 스레드 풀을 1개(`cv2.setNumThreads(1)`)로 제한하여 5개 이상 다중 인스턴스 동시 구동 시 CPU 컨텍스트 스위칭 폭증 및 UI 프리징(응답 없음/뻗음) 현상 방지
  - 멀티스레드 환경 내 GPU 드라이버 충돌 방지를 위한 OpenCL 비활성화(`cv2.ocl.setUseOpenCL(False)`) 적용

### 🔄 Changed (기본값 변경)
- **기본 스캔 간격**: `2.0초` → **`1.0초`**로 단축
- **기본 이미지 유사도 임계값**: `0.8 (80%)` → **`0.9 (90%)`**로 상향

---

## [v0.3.6] - 2026-08-22

### 🚀 Added (추가)
- **동일 템플릿 연속 매칭 경고 기능**:
  - 특정 템플릿이 지정한 횟수 이상 연속으로 매칭되어 화면이 정체되거나 무한 루프에 빠진 경우를 감지
  - `⚙️ Settings` 창에 `동일 템플릿 연속 매칭 경고 횟수 (0: 끄기)` 설정 항목 추가
  - 경고 발생 시 탭 리본 강조(`⚠️ [탭이름]`), 상단 헤더 프레임 붉은색 전환, 상태 레이블 변경 및 모달 팝업 알림 창 표시
  - 다른 템플릿 인식 시 연속 카운트 초기화 및 정상 상태(초록색)로 자동 복구
- **실시간 타이머 카드 연속 횟수 표시**:
  - 동일 템플릿이 2회 이상 연속 매칭 중일 때 `🎯 최근 매칭: close_btn (연속 3회) (1초 전)` 형태로 실시간 표기
- **다중 인스턴스 전역 설정 공유 단위 테스트**:
  - `test_multiple_instances_share_same_settings` 및 연속 매칭 카운터/경고 테스트 추가 (총 90개 테스트 전원 통과)

### 🔄 Changed (변경)
- **인스턴스 전역 공통 설정 동기화**:
  - 기존에 탭(인스턴스)별로 개별 분리 관리되던 모든 동작 설정(스캔 간격, 더블클릭 간격, 동작 후 대기 시간, 유사도 임계값, 그레이스케일 스위치, 매칭없음 알림 시간, 미매칭/복구 동작 등)을 **단일 전역 공통 설정(Single Source of Truth)**으로 통합
  - 어느 탭에서든 설정을 변경하면 열려 있는 모든 인스턴스 탭과 `clicker` 객체에 실시간 즉시 반영
  - `config.json` 저장 구조 최적화: 최상위 공통 설정으로 통합 저장하고 인스턴스 목록은 디바이스 연결 주소 목록(`[{"device_address": "..."}]`)으로 관리

### ⚡ Performance (성능 최적화)
- **UI 무중단 In-Place 갱신 (Zero-Lag)**:
  - 템플릿 순서 변경(우선순위 드래그 앤 드롭), 딜레이 변경, 액션 모드 토글(Click ↔ Double ↔ Back), 삭제/추가 시 전체 탭 위젯 파괴/재생성(`widget.destroy()`) 및 디스크 재로드(`load_config()`)를 완전히 제거
  - 개별 위젯 속성만 직접 변경(In-place configuration)하여 버벅임 없이 60fps 부드러운 반응속도 구현

### 🐛 Fixed (버그 수정)
- **PyInstaller Python 3.14 호환성 해결**:
  - Python 3.14 Windows 환경에서 Tcl/Tk 9 zipfs(`libtcl9.0.4.zip`, `libtk9.0.4.zip`)로 인한 `FileNotFoundError: Tcl data directory not found` 빌드 오류 수정 (`TeraboxClicker.spec` 내 자동 압축 해제 및 패키징 번들링 적용)
- **타임아웃 경고 해제 오류 수정**:
  - CustomTkinter에서 `border_color="transparent"` 호출 시 발생하던 `ValueError` 예외 수정 및 탭 리본/헤더 복구 정상화

---

## [v0.3.5] - 2026-08-20

### 🚀 Added (추가)
- **템플릿별 동작 전/후 지연 시간 (Action Delay) 설정**:
  - 템플릿 매칭 직후 클릭 전 대기(`동작 전 대기`) 및 클릭 완료 후 다음 검사 전 대기(`동작 후 대기`) 선택 지원
  - 템플릿 목록 행에 `⏱️ 0s` / `⏱️전 1s` / `⏱️후 2s` 상태 버튼 및 전용 설정 팝업창(`TemplateDelayWindow`) 제공
- **전역 동작 후 공통 대기 시간 (Post-Action Delay)** 설정 항목 추가 (기본값: `2.0초`)
- **템플릿 이미지 썸네일 미리보기 (Hover Preview Popup)**:
  - 템플릿 목록 행에 마우스 커서를 올리면 원본 썸네일, 이미지 해상도, 클릭 오프셋 좌표 정보 플로팅 표시

### ⚡ Performance (성능 최적화)
- **축소 탐색 + 원본 국소 검증 (Coarse-to-Fine Search)**:
  - 저해상도 축소 화면에서 상위 후보 좌표를 먼저 탐색한 뒤 원본 해상도에서 국소 검증하여 CPU 부하 대폭 절감
- **템플릿별 동적 ROI (Dynamic Region of Interest)**:
  - 매칭 성공 위치 주변을 다음 검사 영역으로 재사용
- **화면 변경 감지 및 정지 화면 스캔 생략 (Frame Change Detection)**:
  - 연속된 화면의 CRC 시그니처가 일치하는 경우 불필요한 전체 템플릿 매칭 검사를 안전하게 스킵
- **로그 버퍼 롤링 최적화**:
  - Activity Log 위젯의 텍스트 누적으로 인한 메모리/UI 저하를 방지하기 위해 150줄 초과 시 자동 정리

---

## [v0.3.4] - 2026-08-20

### 🚀 Added (추가)
- **미매칭 복구 최종 예외 동작 (Final Fallback Action)**:
  - 미매칭 템플릿 목록까지 모두 실패했을 때 실행할 최종 안전장치 동작(특정 좌표 클릭, 더블클릭, 뒤로가기, 랜덤 클릭) 패널 추가
  - 화면 캡처 기반 커스텀 좌표 선택(`🎯 좌표 선택`) 및 `⚡ 즉시 실행` 테스트 기능 제공

---

## [v0.3.3] - 2026-08-19

### 🚀 Added (추가)
- **실시간 타이머 및 상태 정보 바 (Status Cards)**:
  - 각 탭 헤더에 미매칭 대기 시간, 매칭없음 경고 시간, 최근 매칭 템플릿 정보를 0.1초 단위로 실시간 카운트다운 표시
- **미매칭 자동 동작 시스템 (No-Match Action)**:
  - 일정 시간 동안 기본 템플릿이 인식되지 않을 때 2차 복구 템플릿 목록 순차 탐색 또는 랜덤 클릭/좌표 클릭 자동 수행
- **스마트 디바이스 스캔 및 자동 중복 제거**:
  - 포트 번호 기반 에뮬레이터 인스턴스 자동 감지 및 IP 형식 정규화

---

## [v0.3.0] - 2026-08-19

### 🚀 Added (추가)
- **다중 인스턴스 탭 인터페이스 (Multi-Tab Interface)**:
  - 여러 개의 안드로이드 에뮬레이터를 동시에 독립적으로 제어할 수 있는 탭 인터페이스 도입
- **상단 글로벌 툴바**:
  - 전체 연결(`🔗`), 전체 시작(`▶`), 전체 중지(`⏹`), 탭 추가(`➕`) 일괄 제어 기능
- **템플릿 등록 2단계 마법사**:
  - 인식 영역 선택(1단계) 및 타겟 클릭 오프셋 좌표 지정(2단계) 분리 지원
- **우선순위 드래그 앤 드롭 (`☰`)** 및 **템플릿 더블클릭 즉시 실행** 기능 지원
- **단일 원자적 설정 파일 저장 (Atomic JSON Save)** 구조 도입
