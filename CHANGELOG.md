# 📜 Changelog (변경 이력)

모든 주요 변경 사항은 이 문서에 기록됩니다.  
이 프로젝트는 [Semantic Versioning](https://semver.org/lang/ko/) 규칙을 따릅니다.

## [v0.3.9] - 2026-08-23

### 🚀 Added (추가)
- **실시간 템플릿 검색 및 필터링 창 (Template Search Bar)**:
  - 기본 템플릿 및 미매칭 복구 템플릿 탭 상단에 실시간 검색창(`🔍 템플릿 검색...`)과 원클릭 초기화 버튼(`✕`) 추가
  - 검색어 입력 시 대소문자 구분 없이 실시간으로 일치하는 템플릿만 필터링하여 노출
  - 검색 결과가 없을 시 '🔍 일치하는 템플릿이 없습니다.' 안내 레이블 표시
  - 전역 단축키 `Ctrl+F` 지원으로 현재 활성 탭의 검색창으로 즉시 포커스 이동, `Escape` 키 입력 시 검색어 즉시 초기화
  - 템플릿 추가, 삭제, 순서 변경 및 새로고침 시에도 검색 필터 상태 유지

### ⚡ Performance (성능 최적화)
- **초경량 64비트 샘플링 해시(Fast Screen Hash) 기반 정지 화면 스킵**:
  - 기존 수 메가바이트(6.2MB) 전체 이미지 버퍼에 대한 무거운 CRC32 연산을 배제하고, 8x8 균등 격자 샘플(64~192바이트) 기반 초고속 64비트 FNV-1a 샘플링 해시(`_fast_screen_hash`)를 도입하여 화면 변화 감지 연산 시간을 0.005ms 수준으로 단축
  - 화면이 정지되어 있거나 로딩 중일 때 불필요한 OpenCV 템플릿 매칭 루프를 즉시 생략하여 유휴 시 CPU 사용량을 80% 이상 절감
- **비활성(백그라운드) 탭 UI 타이머 렌더링 스로틀링**:
  - 사용자가 현재 보고 있는 활성 탭은 0.5초 주기로 고속 갱신하고, 비활성 백그라운드 탭은 0.5초 경고 상태 검사만 수행하며 UI 텍스트 렌더링을 2.5초 주기로 지능적 스로틀링하여 Tkinter 메인 스레드 렌더링 부하 대폭 절감

### 🐛 Fixed (버그 수정)
- **다중 인스턴스 환경에서 템플릿 삭제 시 타 인스턴스 로드 실패 및 로그 스팸 방지**:
  - 어떤 탭에서 템플릿을 삭제했을 때, 모든 실행 중인 인스턴스(`Clicker`)의 메모리(`template_order`, `fallback_template_order` 및 카운트/액션/딜레이 등)에서도 해당 템플릿이 즉시 제거되도록 동기화 보완
  - 백그라운드 스캔 루프에서 디스크에 존재하지 않는 템플릿 파일이 감지될 경우 에러 로그 스팸 없이 자동으로 스캔 목록에서 정리(Auto-pruning) 및 스킵하도록 안전 로직 강화
  - `_load_template`에서 파일이 디스크에 없는 경우와 실제 파일은 있으나 손상된 경우를 명확히 구분하여, 삭제된 파일로 인한 반복적인 '템플릿 로드 실패' 로그 출력을 원천 차단

---

## [v0.3.8] - 2026-08-23

### 🎨 UI/UX & Design System (디자인 및 사용성 개선)
- **눈 피로도를 최소화한 소프트 뮤트 다크 테마 팔레트 전면 개편**:
  - 눈부심과 색수차를 유발하던 쨍한 네온 레드(`COLOR_DANGER`)를 깊이감 있고 차분한 소프트 크림슨 와인(`"#8A2E3B"`)으로 전면 교체
  - 카운트 초기화 등 상시 노출되는 보조 버튼에 서브 다크 와인 톤(`"#48262D"`) 적용
  - 블루, 그린, 앰버, 인디고, 슬레이트 등 전 색상 토큰을 장시간 사용에 편안한 모던 다크 톤으로 일체화
- **4대 카드 카테고리 기반 설정창(SettingsWindow) 전면 개편**:
  - 기존의 복잡한 나열 방식을 `⏱️ 클릭 및 타이밍`, `🎯 이미지 매칭 알고리즘`, `⚠️ 경고 및 카운터`, `⚡ 미매칭 자동 복구 동작`의 4개 카드 컨테이너로 그룹화
  - 전폭 입력 필드를 컴팩트한 좌측 라벨 / 우측 입력창(75px) 1:1 수평 정렬 행 레이아웃으로 개선
  - `CTkScrollableFrame`을 적용하여 다양한 해상도 및 윈도우 배율(125%, 150%)에서도 잘림 없이 부드러운 스크롤 지원
- **설정 버튼 프로그램 최상단 툴바 우측(`Top-Right`) 이동**:
  - 전역 설정을 어느 탭에서든 한 번의 클릭으로 즉시 열 수 있도록 상단 툴바 우측으로 재배치
  - 개별 탭 헤더에서 중복 설정 버튼을 제거하여 탭 헤더 공간 최적화
- **버튼 및 텍스트 레이아웃 규격화**:
  - 이모지 폰트 글리프 폭 불일치로 인한 텍스트 밀림 현상을 전면 교정
  - 템플릿 우클릭 메뉴(`CTKContextMenu`)에서 아이콘을 제거하고 좌측 기준선(`anchor="w"`)에 완벽하게 일치하는 미니멀 텍스트 메뉴로 개선
  - 템플릿 목록 행의 액션 토글, 지연 시간, 카운트 라벨, 글로벌 툴바 버튼 텍스트 표준화

### 🚀 Added (추가)
- **에뮬레이터 인스턴스 초고속 병렬 소켓 탐색기**:
  - LDPlayer, BlueStacks, Nox, MuMu 6/12/X, MEmu 등 모든 주요 앱플레이어의 40여 개 포트 대역을 비동기 병렬 TCP 소켓 검사로 0.05초 내 선탐색 후 열려 있는 포트에만 `adb connect`를 수행하도록 대폭 개선
- **오픈소스 라이선스 및 ADB Apache 2.0 법적 고지 완비**:
  - `LICENSE-adb.txt` (Apache License 2.0 전문 및 AOSP 저작권 고지문) 추가
  - `LICENSE` (MIT 라이선스 및 서드파티 라이선스 고지문) 추가
  - `README.md` 내 라이선스 및 AS-IS 무보증 면책 조항 섹션 신설
  - `SettingsWindow` 내 `📜 오픈소스 라이선스 고지 (Licenses)` 버튼 및 전용 뷰어 모달(`LicenseNoticeWindow`) 탑재
  - PyInstaller `TeraboxClicker.spec` 빌드 시 라이선스 파일 자동 번들링 적용

### 🐛 Fixed (버그 수정)
- **템플릿 이미지 마우스 오버 툴팁 위치 튐 현상 수정**:
  - `TemplatePreviewTooltip`의 좌표 계산을 화면 절대 포인터 좌표(`winfo_pointerx/y`) 기반으로 개선하고 모니터 경계 감지 보호 로직을 적용하여 엉뚱한 위치에 뜨는 문제 해결
- **ADB 디바이스 목록 파싱 오류 수정**:
  - `adb devices` 실행 시 데몬 시작 로그(`* daemon not running...`)가 포함되는 경우 첫 번째 디바이스가 누락되거나 오류를 일으키던 파싱 필터링 결함 수정

---

## [v0.3.7] - 2026-08-23

### 🐛 Fixed (버그 수정)
- **ADB 데몬 자동 실행(Cold Start) 복구**:
  - 로컬 환경(`127.0.0.1`, `localhost`) 및 `start-server` 명령 시 `-H` 플래그를 제외하도록 수정하여, ADB 서버가 꺼져 있는 상태에서도 구버전 도움 없이 정상적으로 백그라운드 ADB 데몬이 자동 기동되도록 해결
  - 한글 윈도우(CP949) 환경에서 ADB 표준 출력 오류 메시지 처리 시 `UnicodeDecodeError` 크래시 방지 (UTF-8 replace 디코딩 적용)
- **템플릿 우선순위 드래그 앤 드롭 실시간 시각적 재배치 반영**:
  - `update_all_template_order` 내 `pack_forget()`을 선행 적용하여 프로그램 재시작 없이도 UI 상에서 템플릿 목록 행의 위아래 순서가 즉시 시각적으로 재배치되도록 수정

### ⚡ Performance (성능 최적화)
- **고속 ADB 직결 TCP 소켓 스트리밍 캡처 (Zero Subprocess)**:
  - 매 프레임마다 `adb.exe` 프로세스를 새로 생성/종료하던 방식을 탈피하고, ADB 서버(포트 5037)와 직접 TCP 소켓 스트림으로 통신하도록 개선하여 윈도우 프로세스 생성(`CreateProcess`) 오버헤드를 100% 제거
- **단일 패스 템플릿 사전 캐싱 (Dual Grayscale & Color In-Memory)**:
  - 템플릿 디코딩 시 컬러와 그레이스케일 변환을 인메모리 단일 패스로 처리하여 디스크 I/O 호출 및 반복 색상 변환 연산을 50% 절감
- **다중 인스턴스 OpenCV 스레드 폭발 방지 및 CPU 최적화**:
  - OpenCV 내부 워커 스레드 풀을 1개(`cv2.setNumThreads(1)`)로 제한하여 5개 이상 다중 인스턴스 동시 구동 시 CPU 컨텍스트 스위칭 폭증 및 UI 프리징(응답 없음/뻗음) 현상 방지
  - 멀티스레드 환경 내 GPU 드라이버 충돌 방지를 위한 OpenCL 비활성화(`cv2.ocl.setUseOpenCL(False)`) 적용

### 🚀 Added (추가)
- **템플릿 우클릭 메뉴 및 이름 변경(Rename) 기능**:
  - 템플릿 항목 우클릭 시 컨텍스트 메뉴 표시 (`✏️ 이름 변경`, `🎯 즉시 실행`, `⏱️ 지연 시간 설정`, `🗑️ 템플릿 삭제`)
  - 템플릿 파일명 변경 시 디스크 상의 이미지 파일명, 매칭 순서, 오프셋, 동작 모드, 지연 시간, ROI 설정 및 모든 인스턴스 탭 UI를 실시간 동기화
- **앱 시작 시 클릭 카운터 자동 초기화 설정**:
  - `⚙️ Settings` 창에 `앱 실행 시 클릭 카운터 자동 초기화 (Reset on Startup)` 스위치 추가
  - 활성화 시 프로그램 실행 시점에 이전에 저장된 모든 기본/복구 템플릿의 누적 클릭 수를 0으로 자동 리셋

### 🔄 Changed (기본값 변경)
- **기본 스캔 간격**: `2.0초` → **`1.0초`**로 단축
- **기본 이미지 유사도 임계값**: `0.8 (80%)` → **`0.9 (90%)`**로 상향

---

## [v0.3.6] - 2026-08-22

### 🚀 Added (추가)
- **동일 템플릿 연속 매칭 경고 기능**:
  - 특정 템플릿이 지정한 횟수 이상 연속으로 매칭되어 화면이 정체되거나 무한 루프에 빠진 경우를 감지
  - `⚙️ Settings` 창에 `동일 템플릿 연속 매칭 경고 횟수 (0: 끄기)` 설정 항목 추가
  - 경고 발생 시 탭 리본 강조(`⚠️ [탭이름]`), 상단 헤더 프레임 붉은색 전환, 상태 레이블 변경 및 모달 팝업 알림 창 표시
  - 다른 템플릿 인식 시 연속 카운트 초기화 및 정상 상태(초록색)로 자동 복구
- **실시간 타이머 카드 연속 횟수 표시**:
  - 동일 템플릿이 2회 이상 연속 매칭 중일 때 `🎯 최근 매칭: close_btn (연속 3회) (1초 전)` 형태로 실시간 표기
- **다중 인스턴스 전역 설정 공유 단위 테스트**:
  - `test_multiple_instances_share_same_settings` 및 연속 매칭 카운터/경고 테스트 추가 (총 90개 테스트 전원 통과)

### 🔄 Changed (변경)
- **인스턴스 전역 공통 설정 동기화**:
  - 기존에 탭(인스턴스)별로 개별 분리 관리되던 모든 동작 설정(스캔 간격, 더블클릭 간격, 동작 후 대기 시간, 유사도 임계값, 그레이스케일 스위치, 매칭없음 알림 시간, 미매칭/복구 동작 등)을 **단일 전역 공통 설정(Single Source of Truth)**으로 통합
  - 어느 탭에서든 설정을 변경하면 열려 있는 모든 인스턴스 탭과 `clicker` 객체에 실시간 즉시 반영
  - `config.json` 저장 구조 최적화: 최상위 공통 설정으로 통합 저장하고 인스턴스 목록은 디바이스 연결 주소 목록(`[{"device_address": "..."}]`)으로 관리

### ⚡ Performance (성능 최적화)
- **UI 무중단 In-Place 갱신 (Zero-Lag)**:
  - 템플릿 순서 변경(우선순위 드래그 앤 드롭), 딜레이 변경, 액션 모드 토글(Click ↔ Double ↔ Back), 삭제/추가 시 전체 탭 위젯 파괴/재생성(`widget.destroy()`) 및 디스크 재로드(`load_config()`)를 완전히 제거
  - 개별 위젯 속성만 직접 변경(In-place configuration)하여 버벅임 없이 60fps 부드러운 반응속도 구현

### 🐛 Fixed (버그 수정)
- **PyInstaller Python 3.14 호환성 해결**:
  - Python 3.14 Windows 환경에서 Tcl/Tk 9 zipfs(`libtcl9.0.4.zip`, `libtk9.0.4.zip`)로 인한 `FileNotFoundError: Tcl data directory not found` 빌드 오류 수정 (`TeraboxClicker.spec` 내 자동 압축 해제 및 패키징 번들링 적용)
- **타임아웃 경고 해제 오류 수정**:
  - CustomTkinter에서 `border_color="transparent"` 호출 시 발생하던 `ValueError` 예외 수정 및 탭 리본/헤더 복구 정상화

---

## [v0.3.5] - 2026-08-20

### 🚀 Added (추가)
- **템플릿별 동작 전/후 지연 시간 (Action Delay) 설정**:
  - 템플릿 매칭 직후 클릭 전 대기(`동작 전 대기`) 및 클릭 완료 후 다음 검사 전 대기(`동작 후 대기`) 선택 지원
  - 템플릿 목록 행에 `⏱️ 0s` / `⏱️전 1s` / `⏱️후 2s` 상태 버튼 및 전용 설정 팝업창(`TemplateDelayWindow`) 제공
- **전역 동작 후 공통 대기 시간 (Post-Action Delay)** 설정 항목 추가 (기본값: `2.0초`)
- **템플릿 이미지 썸네일 미리보기 (Hover Preview Popup)**:
  - 템플릿 목록 행에 마우스 커서를 올리면 원본 썸네일, 이미지 해상도, 클릭 오프셋 좌표 정보 플로팅 표시

### ⚡ Performance (성능 최적화)
- **축소 탐색 + 원본 국소 검증 (Coarse-to-Fine Search)**:
  - 저해상도 축소 화면에서 상위 후보 좌표를 먼저 탐색한 뒤 원본 해상도에서 국소 검증하여 CPU 부하 대폭 절감
- **템플릿별 동적 ROI (Dynamic Region of Interest)**:
  - 매칭 성공 위치 주변을 다음 검사 영역으로 재사용
- **화면 변경 감지 및 정지 화면 스캔 생략 (Frame Change Detection)**:
  - 연속된 화면의 CRC 시그니처가 일치하는 경우 불필요한 전체 템플릿 매칭 검사를 안전하게 스킵
- **로그 버퍼 롤링 최적화**:
  - Activity Log 위젯의 텍스트 누적으로 인한 메모리/UI 저하를 방지하기 위해 150줄 초과 시 자동 정리

---

## [v0.3.4] - 2026-08-20

### 🚀 Added (추가)
- **미매칭 복구 최종 예외 동작 (Final Fallback Action)**:
  - 미매칭 템플릿 목록까지 모두 실패했을 때 실행할 최종 안전장치 동작(특정 좌표 클릭, 더블클릭, 뒤로가기, 랜덤 클릭) 패널 추가
  - 화면 캡처 기반 커스텀 좌표 선택(`🎯 좌표 선택`) 및 `⚡ 즉시 실행` 테스트 기능 제공

---

## [v0.3.3] - 2026-08-19

### 🚀 Added (추가)
- **실시간 타이머 및 상태 정보 바 (Status Cards)**:
  - 각 탭 헤더에 미매칭 대기 시간, 매칭없음 경고 시간, 최근 매칭 템플릿 정보를 0.1초 단위로 실시간 카운트다운 표시
- **미매칭 자동 동작 시스템 (No-Match Action)**:
  - 일정 시간 동안 기본 템플릿이 인식되지 않을 때 2차 복구 템플릿 목록 순차 탐색 또는 랜덤 클릭/좌표 클릭 자동 수행
- **스마트 디바이스 스캔 및 자동 중복 제거**:
  - 포트 번호 기반 에뮬레이터 인스턴스 자동 감지 및 IP 형식 정규화

---

## [v0.3.0] - 2026-08-19

### 🚀 Added (추가)
- **다중 인스턴스 탭 인터페이스 (Multi-Tab Interface)**:
  - 여러 개의 안드로이드 에뮬레이터를 동시에 독립적으로 제어할 수 있는 탭 인터페이스 도입
- **상단 글로벌 툴바**:
  - 전체 연결(`🔗`), 전체 시작(`▶`), 전체 중지(`⏹`), 탭 추가(`➕`) 일괄 제어 기능
- **템플릿 등록 2단계 마법사**:
  - 인식 영역 선택(1단계) 및 타겟 클릭 오프셋 좌표 지정(2단계) 분리 지원
- **우선순위 드래그 앤 드롭 (`☰`)** 및 **템플릿 더블클릭 즉시 실행** 기능 지원
- **단일 원자적 설정 파일 저장 (Atomic JSON Save)** 구조 도입
